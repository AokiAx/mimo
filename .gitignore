# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.coverage
htmlcov/
.cache/

# Distribution / packaging
build/
dist/
*.egg-info/

# Environments
.env
.venv
venv/
env/

# IDE / editor
.idea/
.vscode/
*.swp
*~

# Claude local config (CONTAINS CREDENTIALS — never commit)
.claude/settings.local.json

# Account cookies (sensitive — auth tokens)
accounts/
claw/mimo_cookies.json

# Runtime data (databases, deploy state, logs)
data/*.db
data/*.db-shm
data/*.db-wal
data/auto_deploy.json
data/deploy_history/
data/deploy_logs/

# Gateway secrets & backend config (contains credentials)
data/secrets.json
data/backends.json

# Gateway runtime state (panel CRUD / discovery)
data/model_groups.json
data/probe_nodes.json

# Gateway runtime config (often contains secrets)
gateway.yaml

# Archives / backups / binaries
*.zip
*.tar.gz
*.bak
*.exe

# HAR captures (browser-recorded HTTP archives — usually contain auth tokens)
*.har

# Scratch
.scratch_*

# Captcha dumps from register_mimo / OCR debug (regenerated at runtime)
captcha_*.png
captcha_*.jpg
captcha_*.jpeg
captcha_answer.txt

# Application log files
logs/
verification.md
.codex/
deploy_logs/
tmp/
data/
.runtime_logs/
